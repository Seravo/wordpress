# Docker Compose for Seravo WordPress project template
#
# Start:
#   echo "SITE=wordpress" > .env
#   docker-compose run -e WP_USER_UID=$UID --service-ports --rm wordpress
# Or:
#   docker-compose up
#
# Access:
#   ssh vagrant@wordpress.local -o ForwardAgent=yes
#
# Clean up:
#   docker-compose down -v --remove-orphans
#   rm -rf .vagrant
#
# If container is not visible on your network, expose it with port
# bindings like:
#   - 80:80
#   - 443:443
#   - 22:22
# ...
#
version: '3'

services:
  wordpress:
    container_name: ${SITE:-wordpress}
    hostname: ${SITE:-wordpress}
    image: seravo/wordpress:development
    volumes:
    - wordpress:/data
    - .:/data/wordpress
    entrypoint: /sbin/swd_init
    ports:
      - 80:80
      - 443:443
      - 22:22
    # networks:
    # - traefik-net
    # labels:
    # - "traefik.http.routers.${SITE:-wordpress}_nginx.rule=Host(\"wordpress.docker.localhost\")"
    environment:
    - WP_USER_UID=1001

  # traefik:
  #   container_name: "traefik"
  #   image: traefik:v2.0
  #   command: --api.insecure=true --providers.docker
  #   networks:
  #   - traefik-net
  #   ports:
  #   - 80:80
  #   - 8080:8080
  #   - 8989:8989
  #   volumes:
  #   - /var/run/docker.sock:/var/run/docker.sock
  #   labels:
  #   - "traefik.http.routers.api.rule=Host(\"traefik.docker.localhost\")"

# networks:
#   traefik-net:
#     external: true

volumes:
  wordpress:
